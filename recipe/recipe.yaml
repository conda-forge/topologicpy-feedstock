schema_version: 1

context:
  name: topologicpy
  version: "0.9.1"

package:
  name: ${{ name }}
  version: ${{ version }}

source:
  url: https://github.com/wassimj/topologicpy/archive/refs/tags/v${{ version }}.tar.gz
  sha256: e363ed1ebf0256861155f7501cebf9290f636d0112bc7f339fc73404306f67a4

build:
  number: 0
  noarch: python
  script: pip install . -v --no-deps --ignore-installed --no-build-isolation

requirements:
  host:
    - pip
    - setuptools
    - python ${{ python_min }}.*
  run:
    - python >=${{ python_min }}
    - numpy
    - scipy >=1.4.1
    - numpy >=1.18.0
    - pandas
    - scikit-learn
    - tqdm
    - topologic-core >=7.0.1

tests:
  - python:
      imports:
        - topologicpy
      python_version: ${{ python_min }}.*
      pip_check: false
about:
  license: AGPL-3.0-or-later
  license_file: LICENSE
  summary: The python bindings for topologic
  description: |
    An AI-Powered Spatial Modelling and Analysis Software Library for Architecture, Engineering, and Construction.
  homepage: https://topologic.app/
  repository: https://github.com/wassimj/topologicpy

extra:
  recipe-maintainers:
    - Krande
